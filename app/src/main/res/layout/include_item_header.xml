<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_marginBottom="@dimen/settings_padding_vertical"
    android:background="?attr/selectableItemBackground"
    android:layout_height="150dp">

    <ImageView
        android:id="@+id/item_header_imageView"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:background="?attr/actionBarItemBackground"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="32dp"
        android:alpha="0.8"
        android:src="@drawable/ic_avater" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:id="@+id/item_header_textview"
        android:layout_below="@+id/item_header_imageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="点击登录" />

</RelativeLayout>